package dev.kumbuka.app.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Design tokens pulled from Figma (file v24q8OFYX1D0navj77jrVe, node 1:5551
 * and the variable defs kb/primary + kb/paper). Named to match the Figma
 * variable names so a screen's Figma spec maps directly onto these.
 */
object KbColors {
    val primary = Color(0xFF005860)
    val primaryTint = Color(0xFFD2EEF1)
    val paper = Color(0xFFEFF7F7)
    val paper2 = Color(0xFFE2EEEE)
    val surface = Color(0xFFF9FDFD)
    val ink = Color(0xFF0F1D20)
    val inkMuted = Color(0xFF546062)
    val inkFaint = Color(0xFF899192)
    val border = Color(0xFFD0D9DB)
    val warningTint = Color(0xFFFFE6D3)
    val accent = Color(0xFFDE9C31)
}
